# Install SIWOO.SYSTEM v2

Copy these items into the root of `dltldn1234/dltldn1234`:

- `README.md`
- `assets/`
- `scripts/`
- `.github/`

Then run:

```bash
git add README.md assets scripts .github
git commit -m "feat: launch SIWOO.SYSTEM v2"
git push origin main
```

After the push, open GitHub → Actions → **Update profile activity** → **Run workflow** once.
